# 📚 Платформа деканата - Архитектура проекта

## 🎯 Что уже реализовано (Агент 1 - База системы)

### Модели данных (accounts/models.py)
- **User** - кастомный пользователь (AbstractUser) с полем `role` (STUDENT/TEACHER/DEAN)
- **Student** - профиль студента (связь 1-к-1 с User)
  - Поля: номер зачетки, группа, курс, финансирование, паспорт, адреса, спонсор
  - Методы: `get_average_grade()`, `get_group_rank()` - заглушки
- **Teacher** - профиль преподавателя (связь 1-к-1 с User)
  - Поля: степень, звание, биография, научные интересы, контакты
- **Dean** - профиль декана (связь 1-к-1 с User)
- **Group** - группы студентов (название, курс, год, специальность)
- **GroupTransferHistory** - история переводов студентов между группами

### Аутентификация и управление (accounts/views.py)
- `login_view` - вход в систему
- `profile_view` - просмотр профиля (3 типа: студент/преподаватель/декан)
- `edit_profile` - редактирование профиля
- `change_password` - смена пароля пользователем
- **Функции декана:**
  - `user_management` - список всех пользователей с фильтрами
  - `add_user` - создание нового пользователя (автогенерация логина)
  - `edit_user` - редактирование любого пользователя
  - `reset_password` - сброс пароля деканом
  - `toggle_user_active` - блокировка/активация
  - `transfer_student` - перевод студента в другую группу

### URL-маршруты
- `/accounts/login/` - вход
- `/accounts/profile/` - профиль
- `/accounts/management/` - управление пользователями (только декан)
- `/` - дашборд (редирект после входа)

### Шаблоны (templates/)
- `base.html` - базовый шаблон (Bootstrap 5, синяя цветовая схема)
- `accounts/profile_*.html` - 3 типа профилей
- `accounts/user_management.html` - панель управления деканом
- `core/dashboard_*.html` - 3 типа главных страниц

---

## 🚀 Что нужно следующим агентам

### Агент 2 - Модуль предметов и учебных планов
**Создать:**
- Приложение `subjects`
- Модели: Subject, Curriculum, SubjectAssignment
- Привязка предметов к группам и преподавателям
- Управление учебными планами

**Интеграция:**
- Использовать `Group`, `Teacher`, `Student` из `accounts.models`
- Добавить виджеты на дашборды преподавателя (список групп/предметов)

### Агент 3 - Модуль оценок
**Создать:**
- Приложение `grades`
- Модели: Grade, Assessment, Semester
- Система оценок по предметам
- Расчет среднего балла и рейтинга

**Интеграция:**
- Реализовать методы `Student.get_average_grade()` и `Student.get_group_rank()`
- Добавить виджеты на профиль студента

### Агент 4 - Модуль расписания
**Создать:**
- Приложение `schedule`
- Модели: Lesson, TimeSlot, Classroom
- Расписание занятий для групп и преподавателей

**Интеграция:**
- Добавить виджет "Сегодня" на дашборды преподавателя и студента

### Агент 5 - Модуль посещаемости
**Создать:**
- Приложение `attendance`
- Модели: AttendanceRecord
- Отметка посещений и отчеты

**Интеграция:**
- Реализовать календарь посещений в профиле студента
- Добавить статистику посещаемости

---

## 📁 Структура проекта

```
department_platform/
├── accounts/           # ✅ Готово - Пользователи и профили
├── core/               # ✅ Готово - Главные страницы
├── subjects/           # ⏳ Агент 2
├── grades/             # ⏳ Агент 3
├── schedule/           # ⏳ Агент 4
├── attendance/         # ⏳ Агент 5
├── templates/
│   ├── base.html       # ✅ Базовый шаблон с навигацией
│   ├── accounts/       # ✅ Шаблоны профилей и управления
│   └── core/           # ✅ Дашборды для 3 ролей
├── static/
├── media/
└── manage.py
```

---

## 🔑 Ключевые особенности системы

### Система ролей
```python
user.role == 'STUDENT'  # Студент
user.role == 'TEACHER'  # Преподаватель
user.role == 'DEAN'     # Декан (администратор)
```

### Доступ к профилям
```python
user.student_profile  # для студентов
user.teacher_profile  # для преподавателей
user.dean_profile     # для деканов
```

### Автогенерация логинов
Формат: `{год}502{номер}` → `2025502001`, `2025502002`...

### Декоратор проверки роли
```python
from django.contrib.auth.decorators import user_passes_test

def is_dean(user):
    return user.is_authenticated and user.role == 'DEAN'

@user_passes_test(is_dean)
def dean_only_view(request):
    pass
```

---

## 🎨 UI/UX стандарты

### Цветовая схема
- Основной: `#2563eb` (синий)
- Светлый: `#60a5fa` (голубой)
- Акцент 1: `#10b981` (зеленый)
- Акцент 2: `#f59e0b` (янтарный)

### Карточки (cards)
```html
<div class="card">
    <div class="card-header">
        <i class="bi bi-icon"></i> Заголовок
    </div>
    <div class="card-body">
        Содержимое
    </div>
</div>
```

### Иконки (Bootstrap Icons)
Используются через CDN: `<i class="bi bi-person-circle"></i>`

---

## 🔗 Полезные команды

```bash
# Создать новое приложение
python manage.py startapp app_name

# Применить миграции
python manage.py makemigrations
python manage.py migrate

# Создать суперпользователя (декана)
python manage.py create_dean

# Запуск сервера
python manage.py runserver
```

---

## 📝 Правила разработки

1. **Не изменяй базовые модели** (User, Student, Teacher, Dean) - только расширяй
2. **Используй существующие шаблоны** - наследуй от `base.html`
3. **Добавляй URL в namespace** - `app_name = 'your_app'` в urls.py
4. **Регистрируй приложение** - добавь в `INSTALLED_APPS` в settings.py
5. **Следуй цветовой схеме** - используй CSS переменные из base.html
6. **Добавляй виджеты в дашборды** - не создавай новые главные страницы
7. **Используй декораторы ролей** - для ограничения доступа

---

## ✅ Что работает прямо сейчас

- Вход/выход из системы
- 3 типа профилей с редактированием
- Смена пароля
- Управление пользователями для декана
- Перевод студентов между группами
- Адаптивный интерфейс

## 🎯 Ближайшие задачи

**Приоритет 1:** Модуль предметов (связь преподавателей с группами)
**Приоритет 2:** Модуль оценок (расчет среднего балла)
**Приоритет 3:** Модуль расписания (календарь занятий)
**Приоритет 4:** Модуль посещаемости (отметки и статистика)